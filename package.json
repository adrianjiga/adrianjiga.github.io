{
  "name": "landingpage",
  "version": "1.0.0",
  "description": "Personal landing page built with vanilla HTML, CSS, and JavaScript",
  "license": "ISC",
  "author": "Adrian",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "npm run lint:js && npm run lint:css",
    "lint:js": "eslint js/**/*.js",
    "lint:css": "stylelint css/**/*.css",
    "lint:fix": "npm run lint:js -- --fix && npm run lint:css -- --fix"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "stylelint": "^16.2.0",
    "stylelint-config-standard": "^36.0.0",
    "vite": "^5.1.0"
  }
}